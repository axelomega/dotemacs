;;; host / project specific things.
;;; copy to ~/.emacs.d/

;;; ccls settings
;; (setq my-settings-ccls-executable "<path to ccls executable>")

;;; helm-jira settings
;; (setq my-settings-jira-url        "url to JIRA project")

;;; Projectile
;; (setq my-settings-projectile-generic-command "find . -type f -not -wholename '*some_foler_to_filter/*' -not -wholename '*some_other_folder_to_filter/*' -print0")
